# Backlog

Ideas and stories for future versions. Reviewed when planning the next version.

## v1.2

- Delete projects (currently no way to remove projects)
- Task search — cross-project search by task number and/or title, results in right-side pane, respects visibility, clicking a result switches project and opens task detail

## v1.3

- MCP server integration — let Claude interact with the kanban board (read/create/move tasks)

## v1.2 (continued)

- User deletion — soft delete with preserved identity. Delete owned projects (cascade), transfer team ownership (to member or admin), unassign tasks, remove memberships. Show confirmation with impact summary. Three user states: active, inactive (reversible), deleted (permanent, name preserved).

## Unversioned
- Jenkins/Forgejo integration (waiting for homelab infrastructure to settle)
- Admin visibility of private projects (policy decision — currently admins cannot see private projects)
